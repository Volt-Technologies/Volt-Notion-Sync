node_modules/
dist/
*.log
.env
.env.local
.DS_Store
.tmp-pilot/
.claude/
CLAUDE.md

# Logic App workflow definition holds parameter defaultValues that may
# contain rotated secrets when the user pastes them locally before
# copy-paste deploying to Azure. Keep it untracked so we can't ship a
# secret to a public repo by accident.
azure/notion-to-github-logicapp.json

